#include<bits\stdc++.h>
using namespace std;

int main(){
    int l = min of search space, r = max ; 
    c = 0 or 1 //based on leaning 
    while(l < r){
        int mid = (l+r+c)/2;
        if(target > or >= a[mid])left = mid+x; x = 0 or 1
        if(target < or <= a[mid])right = mid-y;y = 0 or 1
    }
    //if we want to get rid of 
    //right side when target = mid val then
    //right side is unchanging and has equal value
    //also c = 0 since floor value
    //or lean towards left

    //if we want to get rid of 
    //left side when target = mid val then
    //left side is unchanging and has equal value
    //also c = 1 since ceil value
    //lean towards right
    return 0;
}