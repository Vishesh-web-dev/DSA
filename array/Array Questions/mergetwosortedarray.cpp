//merge two sorted array such that resultant array is sorted
// use two pointer approach i.e.
// initialize two pointers each at st. of both array
// is array1's element is greater than insert array2's element
// and increase array2 iterator similarly for array1.
#include<iostream>
#include<iomanip>
using namespace std;
// double roundof(double x,int n){
//     return num.setprecision(n);
// }
int main(){
    double num = 7.316;
    int n = 2;
    cout<<fixed<<setprecision(n)<<num<<endl;
    return 0;
}