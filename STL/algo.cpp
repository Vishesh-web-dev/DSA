#include<bits\stdc++.h>
using namespace std;

int main(){
    //sort(arr,arr+n);
    // sort(itr1,itr2)  sort till itr2 excluded
    //sort(arr.begin(),arr.end());  
    // for decreasing order
    // sort(arr.begin(),arr.end(),greater<int>)
    string s;
    s = "vIshesh";
    stack<int> st;
    
    cout<<st.top();
    return 0;
}